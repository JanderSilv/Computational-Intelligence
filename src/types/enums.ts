export enum robotMovement {
  forward,
  backward,
  left,
  right,
  none,
}

export enum robotMemory {
  go = 0,
  avoid,
  alreadyWalked,
}

export enum objects {
  empty = 0,
  wall,
  dust,
  furniture,
}
